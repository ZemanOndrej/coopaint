{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	"start-server": "node ./src/server/server.js",
    "start": "npm run build-client && npm run start-server",
    "start-prod": "npm install && npm run build-client-prod && npm run start-server",
	"install-client": "cd ./src/client && npm install",
	"build-client": "cd ./src/client && npm run build",
	"build-client-prod": "cd ./src/client && npm run build-prod"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "node-schedule": "^1.3.1",
    "uuid": "^3.3.2",
    "ws": "^6.2.2"
  }
}
